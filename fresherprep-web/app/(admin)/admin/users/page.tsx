import { AdminPlaceholderPage } from "@/components/admin";

export default function AdminUsersPage() {
  return <AdminPlaceholderPage title="Users" description="The backend currently supports changing a known user's role but does not expose a user list or total count. User management awaits that contract." />;
}
